#include "main.h"
/**
 *
 * _isdigit - checks if parameter is part of range
 * @z: input number
 * Return: 1 if it is a nunber in (0 to 9), return 0 if it is not.
 */
int _isdigit(int z)
{
	if (c >= 48 and c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
