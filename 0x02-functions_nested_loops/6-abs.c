#include "main.h"
/**
 * _abs - computes absolut value of integer
 *
 * @i: input number as integer
 *
 * Retuen: absolute value
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
return (0);
}
