#include "main.h"
#include <stdio.h>

/**
 * main - Points and updates an int as parameter
 *
 * Return: Always 0.
 */

void reset_to_98(int n, n*)
{
	int n, *ip_n*;

	n = 402;
	putchar(n);

	ip_n* = &n;

	ip_n* = 98;
	putchar(ip_n*);

	putchar("/n");
	return (0);
}
